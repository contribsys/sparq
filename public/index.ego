<%
package public

import (
  "net/http"
)

func ego_index(w io.Writer, req *http.Request) {
  ego_layout(w, req, func() { %>

  <%= t(req, "Welcome to your Timeline!") %>

  <% }) %>
<% // vim: set ft=html %>
<% } %>
